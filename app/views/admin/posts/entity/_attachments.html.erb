<% if collection.load.any? %>
  <section>
    <h2><%= t('.heading') %></h2>

    <%=
      render(
          partial: 'shared/admin/list',
          locals: {
              collection: collection
          }
      )
    %>
  </section>
<% end %>
